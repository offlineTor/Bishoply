# Optional backend speech synthesis used by the Emma voice endpoint.
edge-tts>=7.0.0
# PostgreSQL runtime adapter and SQLite import tooling.
psycopg[binary]>=3.2.0
redis>=5.0.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
fastapi>=0.115.0
